require("tiny-inline-diagnostic").setup({
    preset = "modern",
    transparent_bg = false,
    transparent_cursorline = false,
    options = {
        multilines = {
            enabled = true,
            always_show = true,
        },
    },
})
